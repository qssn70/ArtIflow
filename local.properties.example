# Optional when ANDROID_HOME is already set.
# sdk.dir=/path/to/Android/Sdk

# Runtime API keys and endpoints are no longer baked into the app at build time.
# Install the app first, then open in-app Settings to edit ARK / OpenSpeech / FlowStudy options.
